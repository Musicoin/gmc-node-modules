os: linux
language: node_js
yarn: 1.7.0
node_js: 8.0.0
go: 1.10.0
services:
  - docker
before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash -s -- --version 1.7.0
  - export PATH="$HOME/.yarn/bin:$PATH"
after_install:
  - yarn publish ./gmc-bin.tgz --new-version 1.0.0
